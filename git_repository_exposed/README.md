# Git Respository Exposed Publicly

## TODO
- Analysis git search all repos ` git grep ".*<regex>.*" $(git rev-list --all)` eg. for 'secret', 'password'
- Analysis Check who committed from commit messages (lookup username)
